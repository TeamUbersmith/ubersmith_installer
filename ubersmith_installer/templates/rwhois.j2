service rwhois
{
	disable = no
	type = UNLISTED
	socket_type = stream
	protocol = tcp
	wait = no
	user = ubersmith
	server = {{ ubersmith_root }}/app/www/rwhois.php
	server_args = {{ main_virtual_host }}
	port = 4321
}
