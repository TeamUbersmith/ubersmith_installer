#!/bin/sh

for DOMAIN in $RENEWED_DOMAINS;
do
        cp /etc/letsencrypt/live/$DOMAIN/cert.pem /opt/certbot/deploy/$DOMAIN.pem;
        cp /etc/letsencrypt/live/$DOMAIN/privkey.pem /opt/certbot/deploy/$DOMAIN.key;
	cp /etc/letsencrypt/live/$DOMAIN/chain.pem /opt/certbot/deploy/cabundle.pem;
done
